import 'package:flutter/foundation.dart';
import 'package:flutter/widgets.dart';

class KabanColumn extends StatelessWidget {
  const KabanColumn({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}